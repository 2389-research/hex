- a "what's happening" line (while assistant and all subtasks are busy 
explaining what's happening, as well as `esc to interrupt` or the like 
X we have a few things with a highlight like the "Send a message..." which is hard 
to read.. kill the highlight and let it just be subtle. 
X shrink the input box 
X support shift+enter for new lines - immediate "thinking" response 
X esc should be the same as deny tool 
X ctrl+c on tool approval is weird 
X suggestions interface seems weird to have at all. 
- deny tool usage isn't 
handled correctly 
- have a build number 
- add an /info command (build, model, session, cwd, tokens, cost) 
- if we have background processes we need a whole interface for dealing with that. 
- ctrl+c in stream should better clean up. 
- implement different views X overall frame needs cleaned 
- clean up and shrink the convo interface 
- vim bindings 
- keyboard message navigation (imagine `Esc:m` to enter messages then j/k)
- 'scroll position' on right side
